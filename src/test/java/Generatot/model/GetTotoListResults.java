/*
 * WebApi.Frontend
 * WebApi.Frontend
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package Generatot.model;

import Generatot.JSON;
import com.google.gson.*;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;

import java.io.IOException;
import java.util.*;

/**
 * GetTotoListResults
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-03-25T19:49:20.010092+03:00[Europe/Moscow]")
public class GetTotoListResults {
  public static final String SERIALIZED_NAME_TOTO_GROUPS = "TotoGroups";
  @SerializedName(SERIALIZED_NAME_TOTO_GROUPS)
  private List<TotoGroup> totoGroups = new ArrayList<>();

  public static final String SERIALIZED_NAME_SPORTS = "Sports";
  @SerializedName(SERIALIZED_NAME_SPORTS)
  private List<SportNode> sports = new ArrayList<>();

  public GetTotoListResults() {
  }

  public GetTotoListResults totoGroups(List<TotoGroup> totoGroups) {
    this.totoGroups = totoGroups;
    return this;
  }

  public GetTotoListResults addTotoGroupsItem(TotoGroup totoGroupsItem) {
    if (this.totoGroups == null) {
      this.totoGroups = new ArrayList<>();
    }
    this.totoGroups.add(totoGroupsItem);
    return this;
  }

   /**
   * Get totoGroups
   * @return totoGroups
  **/
  @javax.annotation.Nonnull
  public List<TotoGroup> getTotoGroups() {
    return totoGroups;
  }

  public void setTotoGroups(List<TotoGroup> totoGroups) {
    this.totoGroups = totoGroups;
  }


  public GetTotoListResults sports(List<SportNode> sports) {
    this.sports = sports;
    return this;
  }

  public GetTotoListResults addSportsItem(SportNode sportsItem) {
    if (this.sports == null) {
      this.sports = new ArrayList<>();
    }
    this.sports.add(sportsItem);
    return this;
  }

   /**
   * Get sports
   * @return sports
  **/
  @javax.annotation.Nonnull
  public List<SportNode> getSports() {
    return sports;
  }

  public void setSports(List<SportNode> sports) {
    this.sports = sports;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    GetTotoListResults getTotoListResults = (GetTotoListResults) o;
    return Objects.equals(this.totoGroups, getTotoListResults.totoGroups) &&
        Objects.equals(this.sports, getTotoListResults.sports);
  }

  @Override
  public int hashCode() {
    return Objects.hash(totoGroups, sports);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class GetTotoListResults {\n");
    sb.append("    totoGroups: ").append(toIndentedString(totoGroups)).append("\n");
    sb.append("    sports: ").append(toIndentedString(sports)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("TotoGroups");
    openapiFields.add("Sports");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("TotoGroups");
    openapiRequiredFields.add("Sports");
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to GetTotoListResults
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!GetTotoListResults.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in GetTotoListResults is not found in the empty JSON string", GetTotoListResults.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!GetTotoListResults.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `GetTotoListResults` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : GetTotoListResults.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the json data is an array
      if (!jsonObj.get("TotoGroups").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `TotoGroups` to be an array in the JSON string but got `%s`", jsonObj.get("TotoGroups").toString()));
      }

      JsonArray jsonArraytotoGroups = jsonObj.getAsJsonArray("TotoGroups");
      // validate the required field `TotoGroups` (array)
      for (int i = 0; i < jsonArraytotoGroups.size(); i++) {
        TotoGroup.validateJsonElement(jsonArraytotoGroups.get(i));
      };
      // ensure the json data is an array
      if (!jsonObj.get("Sports").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `Sports` to be an array in the JSON string but got `%s`", jsonObj.get("Sports").toString()));
      }

      JsonArray jsonArraysports = jsonObj.getAsJsonArray("Sports");
      // validate the required field `Sports` (array)
      for (int i = 0; i < jsonArraysports.size(); i++) {
        SportNode.validateJsonElement(jsonArraysports.get(i));
      };
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!GetTotoListResults.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'GetTotoListResults' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<GetTotoListResults> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(GetTotoListResults.class));

       return (TypeAdapter<T>) new TypeAdapter<GetTotoListResults>() {
           @Override
           public void write(JsonWriter out, GetTotoListResults value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public GetTotoListResults read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of GetTotoListResults given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of GetTotoListResults
  * @throws IOException if the JSON string is invalid with respect to GetTotoListResults
  */
  public static GetTotoListResults fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, GetTotoListResults.class);
  }

 /**
  * Convert an instance of GetTotoListResults to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

