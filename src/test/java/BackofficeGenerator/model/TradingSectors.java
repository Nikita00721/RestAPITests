/*
 * Backoffice.Api
 * Backoffice.Api
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package BackofficeGenerator.model;

import BackofficeGenerator.JSON;
import com.google.gson.*;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;

import java.io.IOException;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/**
 * TradingSectors
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-03-26T23:02:24.556418400+03:00[Europe/Moscow]")
public class TradingSectors {
  public static final String SERIALIZED_NAME_PRICES_ENABLED = "PricesEnabled";
  @SerializedName(SERIALIZED_NAME_PRICES_ENABLED)
  private Boolean pricesEnabled;

  public static final String SERIALIZED_NAME_LIMITS_ENABLED = "LimitsEnabled";
  @SerializedName(SERIALIZED_NAME_LIMITS_ENABLED)
  private Boolean limitsEnabled;

  public TradingSectors() {
  }

  public TradingSectors pricesEnabled(Boolean pricesEnabled) {
    this.pricesEnabled = pricesEnabled;
    return this;
  }

   /**
   * Get pricesEnabled
   * @return pricesEnabled
  **/
  @javax.annotation.Nullable
  public Boolean getPricesEnabled() {
    return pricesEnabled;
  }

  public void setPricesEnabled(Boolean pricesEnabled) {
    this.pricesEnabled = pricesEnabled;
  }


  public TradingSectors limitsEnabled(Boolean limitsEnabled) {
    this.limitsEnabled = limitsEnabled;
    return this;
  }

   /**
   * Get limitsEnabled
   * @return limitsEnabled
  **/
  @javax.annotation.Nullable
  public Boolean getLimitsEnabled() {
    return limitsEnabled;
  }

  public void setLimitsEnabled(Boolean limitsEnabled) {
    this.limitsEnabled = limitsEnabled;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TradingSectors tradingSectors = (TradingSectors) o;
    return Objects.equals(this.pricesEnabled, tradingSectors.pricesEnabled) &&
        Objects.equals(this.limitsEnabled, tradingSectors.limitsEnabled);
  }

  @Override
  public int hashCode() {
    return Objects.hash(pricesEnabled, limitsEnabled);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TradingSectors {\n");
    sb.append("    pricesEnabled: ").append(toIndentedString(pricesEnabled)).append("\n");
    sb.append("    limitsEnabled: ").append(toIndentedString(limitsEnabled)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("PricesEnabled");
    openapiFields.add("LimitsEnabled");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to TradingSectors
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TradingSectors.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TradingSectors is not found in the empty JSON string", TradingSectors.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!TradingSectors.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `TradingSectors` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TradingSectors.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TradingSectors' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TradingSectors> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TradingSectors.class));

       return (TypeAdapter<T>) new TypeAdapter<TradingSectors>() {
           @Override
           public void write(JsonWriter out, TradingSectors value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TradingSectors read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of TradingSectors given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of TradingSectors
  * @throws IOException if the JSON string is invalid with respect to TradingSectors
  */
  public static TradingSectors fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TradingSectors.class);
  }

 /**
  * Convert an instance of TradingSectors to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

