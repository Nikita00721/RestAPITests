/*
 * Backoffice.Api
 * Backoffice.Api
 *
 * The version of the OpenAPI document: 1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package BackofficeGenerator.model;

import BackofficeGenerator.JSON;
import com.google.gson.*;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import org.openapitools.jackson.nullable.JsonNullable;

import java.io.IOException;
import java.util.*;

/**
 * PlayerProfileLimitGroupItem
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2024-03-26T23:02:24.556418400+03:00[Europe/Moscow]")
public class PlayerProfileLimitGroupItem {
  public static final String SERIALIZED_NAME_LIMIT_GROUP_NAME = "LimitGroupName";
  @SerializedName(SERIALIZED_NAME_LIMIT_GROUP_NAME)
  private String limitGroupName;

  public static final String SERIALIZED_NAME_LIMIT_GROUP_ID = "LimitGroupId";
  @SerializedName(SERIALIZED_NAME_LIMIT_GROUP_ID)
  private Integer limitGroupId;

  public PlayerProfileLimitGroupItem() {
  }

  public PlayerProfileLimitGroupItem limitGroupName(String limitGroupName) {
    this.limitGroupName = limitGroupName;
    return this;
  }

   /**
   * Get limitGroupName
   * @return limitGroupName
  **/
  @javax.annotation.Nullable
  public String getLimitGroupName() {
    return limitGroupName;
  }

  public void setLimitGroupName(String limitGroupName) {
    this.limitGroupName = limitGroupName;
  }


  public PlayerProfileLimitGroupItem limitGroupId(Integer limitGroupId) {
    this.limitGroupId = limitGroupId;
    return this;
  }

   /**
   * Get limitGroupId
   * @return limitGroupId
  **/
  @javax.annotation.Nullable
  public Integer getLimitGroupId() {
    return limitGroupId;
  }

  public void setLimitGroupId(Integer limitGroupId) {
    this.limitGroupId = limitGroupId;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    PlayerProfileLimitGroupItem playerProfileLimitGroupItem = (PlayerProfileLimitGroupItem) o;
    return Objects.equals(this.limitGroupName, playerProfileLimitGroupItem.limitGroupName) &&
        Objects.equals(this.limitGroupId, playerProfileLimitGroupItem.limitGroupId);
  }

  private static <T> boolean equalsNullable(JsonNullable<T> a, JsonNullable<T> b) {
    return a == b || (a != null && b != null && a.isPresent() && b.isPresent() && Objects.deepEquals(a.get(), b.get()));
  }

  @Override
  public int hashCode() {
    return Objects.hash(limitGroupName, limitGroupId);
  }

  private static <T> int hashCodeNullable(JsonNullable<T> a) {
    if (a == null) {
      return 1;
    }
    return a.isPresent() ? Arrays.deepHashCode(new Object[]{a.get()}) : 31;
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class PlayerProfileLimitGroupItem {\n");
    sb.append("    limitGroupName: ").append(toIndentedString(limitGroupName)).append("\n");
    sb.append("    limitGroupId: ").append(toIndentedString(limitGroupId)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("LimitGroupName");
    openapiFields.add("LimitGroupId");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to PlayerProfileLimitGroupItem
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!PlayerProfileLimitGroupItem.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in PlayerProfileLimitGroupItem is not found in the empty JSON string", PlayerProfileLimitGroupItem.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!PlayerProfileLimitGroupItem.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `PlayerProfileLimitGroupItem` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("LimitGroupName") != null && !jsonObj.get("LimitGroupName").isJsonNull()) && !jsonObj.get("LimitGroupName").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `LimitGroupName` to be a primitive type in the JSON string but got `%s`", jsonObj.get("LimitGroupName").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!PlayerProfileLimitGroupItem.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'PlayerProfileLimitGroupItem' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<PlayerProfileLimitGroupItem> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(PlayerProfileLimitGroupItem.class));

       return (TypeAdapter<T>) new TypeAdapter<PlayerProfileLimitGroupItem>() {
           @Override
           public void write(JsonWriter out, PlayerProfileLimitGroupItem value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public PlayerProfileLimitGroupItem read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of PlayerProfileLimitGroupItem given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of PlayerProfileLimitGroupItem
  * @throws IOException if the JSON string is invalid with respect to PlayerProfileLimitGroupItem
  */
  public static PlayerProfileLimitGroupItem fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, PlayerProfileLimitGroupItem.class);
  }

 /**
  * Convert an instance of PlayerProfileLimitGroupItem to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

